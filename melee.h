class melee;
{
public:
  int accuracy;
  int damageMultilpy;
  int speedMultiply;
  int crit;
  //void attack();
  //attack function is on the hero class
  //we need to standardize what functions go where
  void specAttack();
};
